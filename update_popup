#!/usr/bin/sh

(echo "Updates"
while read p; 
do 
    echo "$p"; 
done < ~/updates) | dzen2 -p 4 -x "620" -y "20" -w "200" -l "$(wc -l ~/updates)" -sa 'c' -ta 'c' -e 'onstart=uncollapse;button1=exit;button3=exit' -fn  -*-ohsnapu-medium-r-normal-*-11-*-*-*-*-*-*-* 
